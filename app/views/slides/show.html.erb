<h2><%= @slide.Title%> </h2>

<h3><%=@slide.Instructor%></h3>
<%= link_to "Back", root_path%>

<%if user_signed_in?%>
<%if @slide.user_id== current_user.id%>
<%= link_to "Edit", edit_slide_path(@slide)%>
<%= link_to "Delete", slide_path(@slide), method: :delete, data: { confirm: "Are yous sure?"} %>
<%end%>
<%end%>
